# custom-flappy

Default custom bird asset:
- Place your sprite at assets/bird-reference.png to make it the default bird on load.
- If that file is missing, the game falls back to the built-in preset birds.